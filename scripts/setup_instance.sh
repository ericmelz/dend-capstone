#!/bin/bash

set -ex

# before this is run:
# upload github keys to ~/.ssh
# run ssh-agent
# ssh-add keys
# git clone project

# update, upgrade

# setup all packages:
# postgres
# postgis

# setup directory structure
# /data

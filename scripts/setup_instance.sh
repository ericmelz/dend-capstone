#!/bin/bash

script_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

set -ex

# before this is run:
# upload github keys to ~/.ssh
# run ssh-agent
# ssh-add keys
# git clone project

# update, upgrade

# setup all packages:
# postgres
# postgis

# setup directory structure
# /data
